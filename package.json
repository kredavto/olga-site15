{
  "name": "lumen-vet",
  "version": "1.0.0",
  "private": true,
  "description": "LUMEN VET — премиальный сайт ветеринарной клиники 24/7. Статический генератор без зависимостей.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "preview": "node build.mjs && npx --yes serve dist -l 4173"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
